<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.leese.usercenter.mapper.RiderMapper">

    <!-- 映射数据库字段到 RiderEntity -->
    <resultMap id="BaseResultMap" type="com.leese.usercenter.model.entity.RiderEntity">
        <id property="employeeId" column="employeeId"/>
        <result property="name" column="name"/>
        <result property="role" column="role"/>
        <result property="loginCredentials" column="loginCredentials"/>
        <result property="restaurantId" column="restaurantId"/>
        <result property="phone" column="phone"/>
        <result property="createTime" column="createTime"/>
        <result property="updateTime" column="updateTime"/>
        <result property="isDelete" column="isDelete"/>
    </resultMap>

    <!-- 可复用的列清单 -->
    <sql id="Base_Column_List">
        employeeId, name, role, loginCredentials, restaurantId, phone, createTime, updateTime, isDelete
    </sql>

    <!-- 查询骑手信息 -->
    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM rider
        WHERE employeeId = #{employeeId}
    </select>

</mapper>
